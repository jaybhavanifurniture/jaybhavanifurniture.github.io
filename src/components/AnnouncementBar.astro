---
import promos from '../data/promo.json';
import { Icon } from 'astro-icon/components';

let currentPromo = promos.find((p) => {
  if (!p.isActive || p.campaignType === 'evergreen') return false;
  if (!p.startDate || !p.endDate) return false; // Appease TS compiler
  const now = new Date().getTime();
  const start = new Date(p.startDate).getTime();
  const end = new Date(p.endDate).getTime();
  return now >= start && now <= end;
});

if (!currentPromo) {
  currentPromo = promos.find((p) => p.isActive && p.campaignType === 'evergreen');
}

if (!currentPromo || !currentPromo.title) {
  return null;
}

const { title, href, discountText, endDate } = currentPromo;
---

<a 
  id="announcement-bar"
  href={href}
  class="group relative block bg-amber-500 px-4 py-2.5 text-center text-sm font-semibold text-white shadow-sm hover:bg-amber-600 transition-colors sm:px-6 lg:px-8"
>
  <div class="mx-auto flex max-w-7xl items-center justify-center gap-2">
    <Icon name="tabler:tag" class="h-4.5 w-4.5 shrink-0" />
    <span class="flex-1 text-center sm:flex-none flex items-center justify-center flex-wrap gap-2">
      <span>{title}</span>
      {discountText && <span class="font-bold bg-white/20 px-2 py-0.5 rounded text-xs">{discountText}</span>}
    </span>
    <Icon name="tabler:arrow-right" class="h-4 w-4 shrink-0 transition-transform duration-300 group-hover:translate-x-1" />
  </div>
</a>

<script define:vars={{ endDate }}>
  if (endDate && new Date().getTime() > new Date(endDate).getTime()) {
    const bar = document.getElementById('announcement-bar');
    if (bar) bar.style.display = 'none';
  }
</script>
